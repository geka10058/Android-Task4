<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory android:title="Sorting">
        <ListPreference
            android:key="pref_sort"
            android:title="Sort by:"
            android:summary="Set sorting option"
            android:defaultValue="1"
            android:entries="@array/sort_by"
            android:entryValues="@array/sort_by"
            android:dialogTitle="Выберите стиль для шрифта" />
    </PreferenceCategory>

    <PreferenceCategory android:title="Database type">

        <CheckBoxPreference
            android:defaultValue="true"
            android:key="RoomDB"
            android:title="Room" />
    </PreferenceCategory>

</PreferenceScreen>